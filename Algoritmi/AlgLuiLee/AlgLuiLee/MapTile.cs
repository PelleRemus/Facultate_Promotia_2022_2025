namespace AlgLuiLee
{
    public class MapTile
    {
        public int line, column, value;

        public MapTile(int line, int column, int value)
        {
            this.line = line;
            this.column = column;
            this.value = value;
        }
    }
}
